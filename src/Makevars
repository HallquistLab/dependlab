
## Current RcppArmadillo releases require C++14.  In particular, their headers
## use deduced return types and the [[deprecated]] attribute, both of which are
## unavailable under the previously selected C++11 standard.
CXX_STD = CXX14

#commented this out to avoid -fopenmp errors
#PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS) 
#PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

PKG_CXXFLAGS = 
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS)
